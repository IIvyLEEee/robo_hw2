---
alwaysApply: true
---

# 通用规则

## 训练启动规则

- 启动训练必须使用 `tmux`，不要直接在当前 shell 前台跑长训练。
- 使用hydra override的方式设置不同实验的参数，不要直接改动yaml。
- 训练禁止使用 GPU 0。
- 训练只能使用 GPU 1、GPU 2、GPU 3。
- 每次选择 GPU 前，先运行 `nvidia-smi` 查看 GPU 1/2/3 的显存和利用率。
- 在 GPU 1/2/3 中选择当前最空闲的一张；如果显存和利用率接近，优先选择显存占用更低的 GPU。
- 启动训练时显式设置设备，例如 `training.device=cuda:1` 或 `CUDA_VISIBLE_DEVICES=1`，确保不会占用 GPU 0。
