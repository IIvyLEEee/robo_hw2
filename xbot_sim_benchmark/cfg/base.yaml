headless: false

app:
  headless: ${..headless}
  enable_cameras: ! ${..eval_render}

total_frames: -1
max_iters: -1
eval_interval: -1
eval_render: false
save_interval: -1
seed: 0

camera_follow: true

vla:
  server: null
  trajectory_output_dir: ""

defaults:
  - task: task_base
  - _self_
  
task: {}

replay:
  data_path: ""
  render: false
